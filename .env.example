# Preview mode is self-contained and uses explicitly labeled illustrative fixtures.
MUSEQUEST_DATA_MODE=preview
# Set MUSEQUEST_DATA_MODE=api to read from the Spring backend.
# Restart dev or rebuild production after changing these values.
MUSEQUEST_API_URL=http://127.0.0.1:8080
